import { Query, QueryOptions } from '../../engine';
import { QueryRenderInput } from '../../types';
export declare const useDataQuery: (query: Query, { onComplete, onError, variables, lazy }?: QueryOptions) => QueryRenderInput;
