"use strict";

var _hasValue = require("../hasValue.js");

var _testHelpers = require("../test-helpers");

describe('validator: hasValue', () => {
  describe('should return undefined for allowed values', () => {
    (0, _testHelpers.testValidatorValues)(_hasValue.hasValue, undefined, ['test', false, true, 0, 1, {}, [], new Date()]);
  });
  describe('should return the error message for disallowed values', () => {
    (0, _testHelpers.testValidatorValues)(_hasValue.hasValue, _hasValue.hasValueMessage, ['', undefined, null]);
  });
});