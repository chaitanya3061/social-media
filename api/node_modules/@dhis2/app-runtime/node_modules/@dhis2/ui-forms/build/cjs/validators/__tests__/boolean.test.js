"use strict";

var _boolean = require("../boolean.js");

var _testHelpers = require("../test-helpers");

describe('validator: boolean', () => {
  (0, _testHelpers.allowsEmptyValues)(_boolean.boolean);
  describe('allows boolean values', () => {
    (0, _testHelpers.testValidatorValues)(_boolean.boolean, undefined, [true, false]);
  });
  describe('rejects non-boolean values', () => {
    (0, _testHelpers.testValidatorValues)(_boolean.boolean, _boolean.invalidBooleanMessage, ['text', 3, {}, [], () => {}]);
  });
});