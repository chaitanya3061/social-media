"use strict";

var _string = require("../string.js");

var _testHelpers = require("../test-helpers");

describe('validator: string', () => {
  (0, _testHelpers.allowsEmptyValues)(_string.string);
  describe('allows strings', () => {
    (0, _testHelpers.testValidatorValues)(_string.string, undefined, ['text', '1', '0.15', 'true', 'false']);
  });
  describe('rejects other data types', () => {
    (0, _testHelpers.testValidatorValues)(_string.string, _string.invalidStringMessage, [1, true, {}, [], () => {}]);
  });
});