"use strict";

require("../common/index");

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('there are 5 unread messages', () => {
  cy.fixture('HeaderBar/dashboard').then(response => ({ ...response,
    unreadMessages: 5
  })).as('dashboardFixture');
});
(0, _steps.Then)('the messages link contains an icon with the number 5', () => {
  cy.get('[data-test="headerbar-messages-count"]').should($count => {
    expect($count.text()).to.equal('5');
  });
});