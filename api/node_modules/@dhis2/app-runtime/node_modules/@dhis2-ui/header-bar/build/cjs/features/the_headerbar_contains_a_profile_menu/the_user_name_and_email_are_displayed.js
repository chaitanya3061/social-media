"use strict";

require("../common/index");

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Then)('contains the user name', () => {
  cy.getAll('@meFixture', '[data-test="headerbar-profile-username"]').should(([{
    name
  }, $name]) => {
    expect($name.text()).to.equal(name);
  });
});
(0, _steps.Then)('contains the user email', () => {
  cy.getAll('@meFixture', '[data-test="headerbar-profile-user-email"]').should(([{
    email
  }, $email]) => {
    expect($email.text()).to.equal(email);
  });
});