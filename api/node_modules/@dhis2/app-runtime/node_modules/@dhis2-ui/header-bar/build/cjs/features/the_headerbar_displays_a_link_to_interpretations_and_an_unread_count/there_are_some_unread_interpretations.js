"use strict";

require("../common/index");

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('there are 10 unread interpretations', () => {
  cy.fixture('HeaderBar/dashboard').then(response => ({ ...response,
    unreadInterpretations: 10
  })).as('dashboardFixture');
});
(0, _steps.Then)('the interpretations link contains an icon with the number 10', () => {
  cy.get('[data-test="headerbar-interpretations-count"]').should($count => {
    expect($count.text()).to.equal('10');
  });
});