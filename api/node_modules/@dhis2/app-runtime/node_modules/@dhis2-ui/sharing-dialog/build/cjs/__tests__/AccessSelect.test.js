"use strict";

var _select = require("@dhis2-ui/select");

var _enzyme = require("enzyme");

var _react = _interopRequireDefault(require("react"));

var _accessSelect = require("../access-select.js");

var _sharingConstants = require("../sharing-constants.js");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('SharingDialog widget - AccessSelect component', () => {
  let shallowAccessSelectComponent;
  let props;
  const onChange = jest.fn();

  const getAccessSelectComponent = props => {
    if (!shallowAccessSelectComponent) {
      shallowAccessSelectComponent = (0, _enzyme.mount)( /*#__PURE__*/_react.default.createElement(_accessSelect.AccessSelect, props));
    }

    return shallowAccessSelectComponent;
  };

  beforeEach(() => {
    shallowAccessSelectComponent = undefined;
    props = {
      access: _sharingConstants.ACCESS_VIEW_ONLY,
      accessOptions: [_sharingConstants.ACCESS_VIEW_ONLY, _sharingConstants.ACCESS_VIEW_AND_EDIT],
      disabled: false,
      label: 'Label',
      placeholder: 'Placeholder',
      prefix: 'Prefix',
      showRemoveOption: false,
      onChange
    };
  });
  it('renders a SingleSelectField component ', () => {
    expect(getAccessSelectComponent(props).find(_select.SingleSelectField)).toHaveLength(1);
  });
});