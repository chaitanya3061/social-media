import PropTypes from '@dhis2/prop-types';
import React from 'react';
import { ShareBlock } from './share-block.js';
import { SharingList } from './sharing-list.js';
export const DefaultSharingContent = ({
  sharingSettings,
  onAdd,
  onChange,
  onRemove
}) => /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(ShareBlock, {
  onAdd: onAdd
}), /*#__PURE__*/React.createElement(SharingList, {
  sharingSettings: sharingSettings,
  onChange: onChange,
  onRemove: onRemove
}));
DefaultSharingContent.propTypes = {
  sharingSettings: PropTypes.object.isRequired,
  onAdd: PropTypes.func.isRequired,
  onChange: PropTypes.func.isRequired,
  onRemove: PropTypes.func.isRequired
};