import _JSXStyle from "styled-jsx/style";
import PropTypes from '@dhis2/prop-types';
import React from 'react';
import { avatarStyles } from '../sharing-dialog.styles';
export const Avatar = ({
  name
}) => {
  const nameParts = name.split(' ');
  let initials = nameParts.shift().charAt(0);

  if (nameParts.length) {
    initials += nameParts.pop().charAt(0);
  }

  return /*#__PURE__*/React.createElement("div", {
    className: `jsx-${avatarStyles.__hash}` + " " + "share-details-avatar"
  }, /*#__PURE__*/React.createElement(_JSXStyle, {
    id: avatarStyles.__hash
  }, avatarStyles), /*#__PURE__*/React.createElement("p", {
    className: `jsx-${avatarStyles.__hash}`
  }, initials));
};
Avatar.defaultProps = {
  name: 'USER'
};
Avatar.propTypes = {
  name: PropTypes.string.isRequired
};