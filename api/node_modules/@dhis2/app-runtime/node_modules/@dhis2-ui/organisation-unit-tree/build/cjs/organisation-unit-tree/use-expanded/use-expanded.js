"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useExpanded = void 0;

var _react = require("react");

var _helpers = require("./helpers.js");

/**
 * @param {string[]} initiallyExpanded
 * @param {Function} [onExpand]
 * @param {Function} [onCollapse]
 * @returns {{ expanded: string[], handleExpand: Function, handleCollapse: Function }}
 */
const useExpanded = (initiallyExpanded, onExpand, onCollapse) => {
  const allInitiallyExpandedPaths = (0, _helpers.getInitiallyExpandedPaths)(initiallyExpanded);
  const [expanded, setExpanded] = (0, _react.useState)(allInitiallyExpandedPaths);
  const {
    handleExpand,
    handleCollapse
  } = (0, _helpers.createExpandHandlers)({
    expanded,
    setExpanded,
    onExpand,
    onCollapse
  });
  return {
    expanded,
    handleExpand,
    handleCollapse
  };
};

exports.useExpanded = useExpanded;