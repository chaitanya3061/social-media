#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@dhis2/cli-app-scripts/bin/d2-app-scripts" "$@"
else 
  exec node  "$basedir/../@dhis2/cli-app-scripts/bin/d2-app-scripts" "$@"
fi
