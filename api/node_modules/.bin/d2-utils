#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@dhis2/cli-utils/bin/d2-utils" "$@"
else 
  exec node  "$basedir/../@dhis2/cli-utils/bin/d2-utils" "$@"
fi
