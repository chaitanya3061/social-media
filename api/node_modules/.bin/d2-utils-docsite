#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@dhis2/cli-utils-docsite/bin/d2-utils-docsite" "$@"
else 
  exec node  "$basedir/../@dhis2/cli-utils-docsite/bin/d2-utils-docsite" "$@"
fi
